# Bruce Infrastructure Configuration Template
# Application settings (LLMs, API keys, Connectors, Tools, System Prompts)
# are configured via the Web UI Settings tab and persisted in SQLite.

server:
  port: 8080

redis:
  address: "localhost:6379"
  password: ""
  db: 1
  max_retries: 3

sqlite:
  dsn: "./data/bruce.db"
